# ☁️ Развертывание на облачном хостинге (24/7)

Инструкции для размещения сайта на облачном хостинге, чтобы он работал постоянно, даже когда ваш ноутбук выключен.

## 🎯 Варианты (от простого к сложному)

### 1. Railway.app ⭐ САМЫЙ ПРОСТОЙ (рекомендуется)

**Railway** - простейший способ разместить сайт. Бесплатный тариф есть!

#### Шаги:

1. **Зарегистрируйтесь на Railway:**
   - Перейдите на https://railway.app
   - Войдите через GitHub

2. **Подготовьте проект:**
   - Убедитесь, что файл `Procfile` находится в корне проекта
   - Проверьте `requirements.txt`

3. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Подключите ваш репозиторий

4. **Добавьте PostgreSQL базу данных:**
   - В проекте нажмите "+ New"
   - Выберите "Database" → "PostgreSQL"
   - Railway автоматически создаст переменные окружения

5. **Настройте переменные окружения:**
   - Перейдите в "Variables"
   - Добавьте:
     ```
     SECRET_KEY=your-secret-key-here
     DEBUG=False
     ALLOWED_HOSTS=*.railway.app,your-project.railway.app
     ```
   - База данных уже настроена автоматически!

6. **Запустите деплой:**
   - Railway автоматически обнаружит Django проект
   - Запустит деплой
   - Дождетесь завершения

7. **Ваш сайт готов!**
   - Railway даст вам URL вида: `https://your-project.railway.app`
   - Сайт работает 24/7!

**Цена:** Бесплатно (с ограничениями) или от $5/месяц

---

### 2. Fly.io ⭐ БЕСПЛАТНО

**Fly.io** предоставляет бесплатный тариф с достаточными ресурсами.

#### Шаги:

1. **Установите Fly CLI:**
   ```bash
   # Windows (PowerShell)
   powershell -Command "iwr https://fly.io/install.ps1 -useb | iex"
   
   # Linux/Mac
   curl -L https://fly.io/install.sh | sh
   ```

2. **Зарегистрируйтесь:**
   ```bash
   fly auth signup
   ```

3. **Инициализируйте проект:**
   ```bash
   fly launch
   ```
   - Выберите регион
   - Создайте Postgres базу: `fly postgres create`
   - Подключите базу: `fly postgres attach`

4. **Настройте переменные:**
   ```bash
   fly secrets set SECRET_KEY=your-secret-key
   fly secrets set DEBUG=False
   fly secrets set ALLOWED_HOSTS=your-app.fly.dev
   ```

5. **Деплой:**
   ```bash
   fly deploy
   ```

**Цена:** Бесплатно (с ограничениями)

---

### 3. PythonAnywhere ⭐ ОЧЕНЬ ПРОСТОЙ

**PythonAnyhouse** - простейший хостинг для Python приложений.

#### Шаги:

1. **Зарегистрируйтесь:**
   - Перейдите на https://www.pythonanywhere.com
   - Создайте бесплатный аккаунт

2. **Откройте консоль Bash:**
   - В панели управления выберите "Bash"

3. **Клонируйте проект:**
   ```bash
   git clone https://github.com/yourusername/uniquest.git
   cd uniquest
   ```

4. **Создайте виртуальное окружение:**
   ```bash
   python3.10 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```

5. **Настройте базу данных:**
   - В панели управления перейдите в "Databases"
   - Создайте MySQL базу (бесплатно)
   - Или используйте SQLite (проще)

6. **Настройте веб-приложение:**
   - Перейдите в "Web"
   - Нажмите "Add a new web app"
   - Выберите "Manual configuration" → "Python 3.10"
   - Укажите путь к вашему проекту

7. **Настройте WSGI:**
   - Откройте WSGI файл
   - Замените содержимое на:
   ```python
   import os
   import sys
   
   path = '/home/yourusername/uniquest'
   if path not in sys.path:
       sys.path.insert(0, path)
   
   os.environ['DJANGO_SETTINGS_MODULE'] = 'uniquest.settings'
   
   from django.core.wsgi import get_wsgi_application
   application = get_wsgi_application()
   ```

8. **Настройте статику:**
   - В разделе "Static files"
   - URL: `/static/`
   - Directory: `/home/yourusername/uniquest/staticfiles`

9. **Обновите сайт:**
   - Нажмите кнопку "Reload"
   - Готово!

**Цена:** Бесплатно (с ограничениями) или от $5/месяц

---

### 4. Render.com ⭐ ПРОСТОЙ

**Render** - еще одна простая платформа.

#### Шаги:

1. **Зарегистрируйтесь:**
   - Перейдите на https://render.com
   - Войдите через GitHub

2. **Создайте новый Web Service:**
   - Connect your repository
   - Build Command: `pip install -r requirements.txt && python manage.py collectstatic --noinput`
   - Start Command: `gunicorn uniquest.wsgi:application`

3. **Добавьте PostgreSQL:**
   - Создайте новый PostgreSQL database
   - Render автоматически добавит переменные окружения

4. **Настройте переменные:**
   ```
   SECRET_KEY=your-secret-key
   DEBUG=False
   ALLOWED_HOSTS=your-app.onrender.com
   ```

5. **Деплой:**
   - Render автоматически задеплоит
   - Сайт готов!

**Цена:** Бесплатно (с ограничениями) или от $7/месяц

---

### 5. VPS (Virtual Private Server) 💰 ДЕШЕВО

Если нужен полный контроль, арендуйте VPS.

#### Популярные провайдеры:
- **Hetzner** - от €4/месяц (Германия)
- **DigitalOcean** - от $6/месяц
- **Vultr** - от $6/месяц
- **Contabo** - от $4/месяц

#### Быстрая настройка (Ubuntu):

1. **Подключитесь к серверу:**
   ```bash
   ssh root@your-server-ip
   ```

2. **Установите зависимости:**
   ```bash
   apt update
   apt install python3-pip python3-venv postgresql nginx git -y
   ```

3. **Клонируйте проект:**
   ```bash
   git clone https://github.com/yourusername/uniquest.git
   cd uniquest
   ```

4. **Настройте проект:**
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```

5. **Настройте базу данных:**
   ```bash
   sudo -u postgres createdb uniquestus
   sudo -u postgres createuser uniquest_user
   sudo -u postgres psql -c "ALTER USER uniquest_user WITH PASSWORD 'your_password';"
   sudo -u postgres psql -c "GRANT ALL PRIVILEGES ON DATABASE uniquestus TO uniquest_user;"
   ```

6. **Используйте скрипт автоматической установки:**
   ```bash
   # См. файл deploy_vps.sh
   ```

**Полная инструкция:** См. `DEPLOY_PUBLIC.md`, раздел "Развертывание на VPS/сервере"

---

## 📊 Сравнение платформ

| Платформа | Сложность | Бесплатно | Цена платная | Рекомендация |
|-----------|-----------|-----------|--------------|--------------|
| Railway | ⭐ Очень просто | ✅ Да | $5/мес | ⭐⭐⭐⭐⭐ |
| Fly.io | ⭐⭐ Просто | ✅ Да | $0.02/час | ⭐⭐⭐⭐ |
| PythonAnywhere | ⭐ Очень просто | ✅ Да | $5/мес | ⭐⭐⭐⭐⭐ |
| Render | ⭐⭐ Просто | ✅ Да | $7/мес | ⭐⭐⭐⭐ |
| VPS | ⭐⭐⭐⭐ Сложно | ❌ Нет | $4-6/мес | ⭐⭐⭐ |

## 🎯 Рекомендация

**Для начинающих:** Railway или PythonAnywhere  
**Для опыта:** Fly.io или VPS  
**Для экономии:** VPS (Hetzner/Contabo)

## ⚙️ Общие настройки

Независимо от платформы, нужно настроить:

### Переменные окружения:

```env
SECRET_KEY=your-very-long-random-secret-key-here
DEBUG=False
ALLOWED_HOSTS=your-domain.com,*.railway.app,etc
DB_NAME=uniquestus
DB_USER=postgres_user
DB_PASSWORD=secure_password
DB_HOST=localhost (или предоставленный хост)
DB_PORT=5432
```

### Применить миграции:

```bash
python manage.py migrate
python manage.py collectstatic --noinput
python manage.py createsuperuser
```

## 🚀 Быстрый старт (Railway)

Самый быстрый способ:

1. Зарегистрируйтесь на railway.app
2. Подключите GitHub репозиторий
3. Добавьте PostgreSQL
4. Настройте переменные
5. Готово! Сайт работает 24/7!

---

**Готово! Теперь ваш сайт будет работать постоянно! 🎉**

