# Как запустить команды

## 🚀 На Render (рекомендуется)

1. Откройте [Render Dashboard](https://dashboard.render.com)
2. Выберите ваш сервис `uniquest0web`
3. В меню слева найдите **"Shell"** или **"Console"**
4. Нажмите **"Open Shell"** или **"Connect"**
5. В открывшемся терминале выполните:

```bash
python manage.py create_test_student
```

После выполнения вы увидите:
```
Создана группа: CS-101
Создан пользователь: test_student
...
Тестовый студент создан успешно!
Логин: test_student
Пароль: test123456
```

## 💻 Локально (если есть локальная БД)

Если у вас настроена локальная база данных (SQLite или PostgreSQL):

```bash
# Активируйте виртуальное окружение (если нужно)
# Windows:
venv\Scripts\activate

# Затем выполните команду:
python manage.py create_test_student
```

## 📝 Другие полезные команды

### Создание полных демо-данных:
```bash
python manage.py seed_demo --students 500 --groups 20 --courses 30 --seed 42
```

### Обучение ML-модели:
```bash
python manage.py train_grade_model --save-path=models/grade_model.pkl
```

### Индексация лекций для поиска:
```bash
python manage.py index_lectures
```

## ⚠️ Важно

- **На Render**: Команды нужно выполнять через Render Shell
- **Локально**: Нужна настроенная локальная база данных
- Если видите ошибку подключения к Render БД локально - это нормально, используйте Render Shell

## 🔑 Логин и пароль тестового студента

После выполнения `create_test_student`:
- **Логин**: `test_student`
- **Пароль**: `test123456`

