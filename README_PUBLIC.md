# 🌐 Публичный доступ к UniQuest

## Быстрый старт для публичного доступа

### Вариант 1: ngrok (Самый простой способ) ⭐

1. **Скачайте ngrok:**
   - Перейдите на https://ngrok.com/download
   - Скачайте для вашей ОС
   - Зарегистрируйтесь (бесплатно) и получите токен

2. **Установите ngrok:**
   ```bash
   # Windows: распакуйте и добавьте в PATH
   # Linux/Mac:
   unzip ngrok.zip
   sudo mv ngrok /usr/local/bin/
   ngrok authtoken YOUR_TOKEN
   ```

3. **Запустите Django:**
   ```bash
   python manage.py runserver
   ```

4. **В другом терминале запустите ngrok:**
   ```bash
   ngrok http 8000
   ```

5. **Используйте URL!**
   - Вы получите URL вида: `https://xxxx-xx-xx-xxx-xx.ngrok.io`
   - Этот URL работает из любого места в интернете
   - Поделитесь им с друзьями/коллегами

### Вариант 2: Доступ из локальной сети

Для доступа с других устройств в вашей сети (Wi-Fi):

**Windows:**
```bash
quick_start.bat
```

**Linux/Mac:**
```bash
chmod +x quick_start.sh
./quick_start.sh
```

**Или вручную:**
```bash
python manage.py runserver 0.0.0.0:8000
```

Затем откройте на другом устройстве: `http://ВАШ-IP:8000`

### Вариант 3: Использование скрипта

```bash
python start_public_server.py
```

Этот скрипт автоматически:
- Определит ваш IP адрес
- Запустит сервер на всех интерфейсах
- Покажет все доступные URL

## 🔐 Безопасность

### Для production:

1. **Создайте файл `.env` в корне проекта:**
```env
SECRET_KEY=your-very-long-random-secret-key-here
DEBUG=False
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,your-ip-address
DB_NAME=uniquestus
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
```

2. **Обновите settings.py** (уже сделано):
   - Поддержка переменных окружения
   - Автоматическое определение хостов

3. **Используйте HTTPS** при работе с реальными данными

## 📱 Доступ с мобильных устройств

1. Запустите сервер с доступом из сети:
   ```bash
   python manage.py runserver 0.0.0.0:8000
   ```

2. Узнайте ваш IP:
   - Windows: `ipconfig` (IPv4 адрес)
   - Linux/Mac: `ifconfig` или `ip addr`

3. На мобильном устройстве откройте:
   ```
   http://ВАШ-IP:8000
   ```

4. **Важно:** Устройства должны быть в одной сети Wi-Fi

## 🌍 Публичный доступ из интернета

### Через ngrok (рекомендуется):

1. Установите ngrok (см. выше)
2. Запустите:
   ```bash
   ngrok http 8000
   ```
3. Используйте предоставленный HTTPS URL

### Через Cloudflare Tunnel (бесплатно, без портов):

1. Зарегистрируйтесь на cloudflare.com
2. Добавьте домен (можно бесплатный)
3. Установите cloudflared
4. Создайте туннель (подробнее в DEPLOY_PUBLIC.md)

## 🚀 Развертывание на VPS/сервере

Полная инструкция в файле **DEPLOY_PUBLIC.md**

Кратко:
1. Арендуйте VPS (DigitalOcean, Hetzner, и др.)
2. Установите Ubuntu/Debian
3. Установите PostgreSQL, Python, Nginx
4. Настройте домен
5. Запустите с Gunicorn + Nginx

## ⚠️ Важные моменты

1. **Файрвол:** Убедитесь, что порт 8000 открыт
2. **Роутер:** Может потребоваться проброс портов
3. **Безопасность:** Не используйте DEBUG=True в production
4. **HTTPS:** Используйте SSL для реальных проектов

## 🆘 Решение проблем

### Не могу подключиться из сети:
- Проверьте файрвол Windows/Linux
- Убедитесь, что сервер запущен на `0.0.0.0` (не `127.0.0.1`)
- Проверьте настройки роутера

### Порт занят:
```bash
python manage.py runserver 0.0.0.0:8080
```

### Статика не загружается:
```bash
python manage.py collectstatic --noinput
```

## 📚 Дополнительная информация

- Полная инструкция: **DEPLOY_PUBLIC.md**
- Улучшения проекта: **README_IMPROVEMENTS.md**

## 💡 Рекомендации

1. **Для тестирования:** Используйте ngrok
2. **Для разработки:** Используйте локальную сеть
3. **Для production:** Используйте VPS с доменом и SSL

---

**Готово! Ваш сайт теперь доступен публично! 🎉**

