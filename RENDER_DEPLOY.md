# 🚀 ДЕПЛОЙ НА RENDER - Полная инструкция

## ✅ Всё готово! Проект полностью настроен для Render!

Все файлы уже созданы и настроены:
- ✅ `render.yaml` - конфигурация для автоматического деплоя
- ✅ `Procfile` - команда запуска
- ✅ `requirements.txt` - все зависимости
- ✅ Настройки Django для Render

---

## 🎯 ШАГ 1: Регистрация на Render

1. Перейдите на https://render.com
2. Нажмите **"Get Started for Free"**
3. Войдите через **GitHub** (рекомендуется)

---

## 🎯 ШАГ 2: Подготовка GitHub репозитория

1. Убедитесь, что ваш код в GitHub:
   ```bash
   git add .
   git commit -m "Ready for Render deployment"
   git push origin main
   ```

2. Проверьте наличие файлов:
   - ✅ `render.yaml` (в корне проекта)
   - ✅ `Procfile` (в корне проекта)
   - ✅ `requirements.txt`
   - ✅ `uniquest/settings.py`

---

## 🎯 ШАГ 3: Автоматический деплой через render.yaml

### Вариант A: Автоматический деплой (РЕКОМЕНДУЕТСЯ!)

Render может автоматически создать все сервисы из файла `render.yaml`!

1. В Render Dashboard нажмите **"New +"** → **"Blueprint"**
2. Подключите ваш GitHub репозиторий
3. Выберите файл `render.yaml`
4. Render автоматически создаст:
   - ✅ Web сервис
   - ✅ PostgreSQL базу данных
   - ✅ Все переменные окружения
   - ✅ Подключит базу к сервису

5. Нажмите **"Apply"**
6. Дождитесь завершения деплоя (5-10 минут)
7. **Готово!** Сайт работает!

---

## 🎯 ШАГ 3: Ручной деплой (если нужен больший контроль)

### 3.1 Создайте PostgreSQL базу данных

1. В Render Dashboard нажмите **"New +"**
2. Выберите **"PostgreSQL"**
3. Настройте:
   - **Name:** `uniquest-db`
   - **Plan:** Free (или Starter)
   - **Region:** Frankfurt (ближайший к Казахстану)
   - **Database Name:** `uniquestus`
   - **User:** `uniquest_user`
4. Нажмите **"Create Database"**
5. Подождите создания (2-3 минуты)

### 3.2 Создайте Web Service

1. В Render Dashboard нажмите **"New +"**
2. Выберите **"Web Service"**
3. Подключите ваш GitHub репозиторий
4. Настройте:

   **Basic Settings:**
   - **Name:** `uniquest-web`
   - **Region:** Frankfurt
   - **Branch:** `main`
   - **Root Directory:** (оставьте пустым)
   - **Runtime:** Python 3
   - **Build Command:**
     ```
     pip install -r requirements.txt && python manage.py collectstatic --noinput
     ```
   - **Start Command:**
     ```
     gunicorn uniquest.wsgi:application
     ```

   **Advanced Settings:**
   - **Plan:** Free (или Starter для постоянной работы)
   - **Auto-Deploy:** Yes (автоматический деплой при push)

### 3.3 Добавьте переменные окружения

В настройках Web Service перейдите в **"Environment"** и добавьте:

```
PYTHON_VERSION=3.10.12
DJANGO_SETTINGS_MODULE=uniquest.settings
DEBUG=False
SECRET_KEY=your-secret-key-here (сгенерируйте случайный ключ)
```

### 3.4 Подключите базу данных

1. В настройках Web Service перейдите в **"Environment"**
2. Нажмите **"Link Database"**
3. Выберите созданную базу `uniquest-db`
4. Render автоматически добавит переменную `DATABASE_URL`

### 3.5 Добавьте ALLOWED_HOSTS

В переменных окружения добавьте:
```
ALLOWED_HOSTS=uniquest-web.onrender.com,*.onrender.com
```

Или получите ваш домен после первого деплоя и добавьте его.

---

## 🎯 ШАГ 4: Деплой и настройка

1. Нажмите **"Create Web Service"**
2. Дождитесь завершения сборки (5-10 минут)
3. После успешного деплоя получите URL: `https://uniquest-web.onrender.com`

### 4.1 Примените миграции

1. Откройте **"Shell"** в Render Dashboard (в настройках Web Service)
2. Выполните:
   ```bash
   python manage.py migrate
   ```

### 4.2 Создайте суперпользователя

В том же Shell:
```bash
python manage.py createsuperuser
```

### 4.3 (Опционально) Загрузите тестовые данные

```bash
python generate_kazakhstan_data.py
```

---

## 🎯 ШАГ 5: Обновите ALLOWED_HOSTS

После получения URL обновите переменные окружения:

1. Перейдите в настройки Web Service → **"Environment"**
2. Обновите `ALLOWED_HOSTS`:
   ```
   ALLOWED_HOSTS=your-actual-url.onrender.com,*.onrender.com
   ```
3. Сохраните и подождите перезапуска

---

## ⚙️ Важные настройки

### План (Free vs Starter)

**Free Plan:**
- ✅ Бесплатно
- ⚠️ Сервис "засыпает" после 15 минут неактивности
- ⚠️ Просыпается при первом запросе (но медленно)
- ✅ Достаточно для тестирования

**Starter Plan ($7/месяц):**
- ✅ Сервис всегда работает
- ✅ Быстрый отклик
- ✅ Для production использования

### Автоматический деплой

Render автоматически деплоит при каждом push в GitHub:
- Перейдите в **"Settings"** → **"Auto-Deploy"**
- Убедитесь, что включено **"Yes"**

---

## 🔒 Безопасность

### Обязательно для production:

1. **Установите DEBUG=False:**
   - В переменных окружения: `DEBUG=False`

2. **Используйте надежный SECRET_KEY:**
   ```python
   # Сгенерируйте новый ключ:
   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
   ```
   Добавьте в переменные окружения

3. **Настройте ALLOWED_HOSTS:**
   - Укажите только ваш домен Render

---

## 📝 Что происходит при деплое

1. Render клонирует ваш репозиторий
2. Устанавливает зависимости из `requirements.txt`
3. Собирает статику: `python manage.py collectstatic --noinput`
4. Запускает сервер: `gunicorn uniquest.wsgi:application`
5. Подключается к PostgreSQL базе данных
6. Сайт доступен по URL!

---

## 🆘 Решение проблем

### Сайт не запускается:

1. **Проверьте логи:**
   - В Render Dashboard → "Logs"
   - Ищите ошибки в логах

2. **Проверьте переменные окружения:**
   - Убедитесь, что все нужные переменные установлены
   - Особенно `DATABASE_URL` и `SECRET_KEY`

3. **Проверьте Build Command:**
   - Должно быть: `pip install -r requirements.txt && python manage.py collectstatic --noinput`

4. **Проверьте Start Command:**
   - Должно быть: `gunicorn uniquest.wsgi:application`

### База данных не подключается:

1. Убедитесь, что база данных создана
2. Проверьте, что она подключена к Web Service
3. Проверьте переменную `DATABASE_URL` в Environment

### Статика не загружается:

1. Проверьте, что `collectstatic` выполняется в Build Command
2. Убедитесь, что WhiteNoise установлен в requirements.txt
3. Проверьте настройки в settings.py

### Миграции не применены:

1. Откройте Shell в Render
2. Выполните: `python manage.py migrate`

---

## ✅ Чеклист перед деплоем

- [ ] Код загружен в GitHub
- [ ] Файл `render.yaml` в корне проекта
- [ ] Файл `Procfile` в корне проекта
- [ ] `requirements.txt` актуален
- [ ] База данных создана на Render
- [ ] Переменные окружения настроены
- [ ] Build Command правильный
- [ ] Start Command правильный

---

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет:
- ✅ Работать на Render
- ✅ Доступен по постоянному URL
- ✅ Работать 24/7 (на Starter плане)
- ✅ Автоматически обновляться при push

**URL будет:** `https://uniquest-web.onrender.com` (или ваш кастомный)

---

## 📚 Дополнительная информация

- **Render Docs:** https://render.com/docs
- **Django на Render:** https://render.com/docs/deploy-django
- **PostgreSQL на Render:** https://render.com/docs/databases

---

## 💡 Советы

1. **Используйте render.yaml** - это самый простой способ
2. **Начните с Free плана** - можно перейти на Starter позже
3. **Проверяйте логи** - там видно все ошибки
4. **Используйте Shell** - для выполнения команд Django

**Удачи с деплоем! 🚀**

