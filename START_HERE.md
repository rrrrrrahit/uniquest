# 🚀 БЫСТРЫЙ СТАРТ - Публичный доступ к UniQuest

## ⚡ НУЖЕН САЙТ, КОТОРЫЙ РАБОТАЕТ 24/7 БЕЗ ВАШЕГО НОУТБУКА?

👉 **Переходите сразу к:** `QUICK_DEPLOY.md` - там инструкция для деплоя на Railway/PythonAnywhere!

Сайт будет работать постоянно, даже когда ноутбук выключен! ☁️

---

## Для временного тестирования (ноутбук должен быть включен)

### Самый простой способ сделать сайт доступным публично

### ⚡ Вариант 1: ngrok (5 минут) - РЕКОМЕНДУЕТСЯ

**ngrok** создает безопасный HTTPS туннель к вашему локальному серверу - это самый простой способ!

#### Шаги:

1. **Скачайте ngrok:**
   - Перейдите на https://ngrok.com/download
   - Скачайте для Windows/Linux/Mac
   - Зарегистрируйтесь (бесплатно)

2. **Установите и настройте:**
   ```bash
   # Распакуйте ngrok
   # Получите токен на сайте ngrok.com
   ngrok authtoken YOUR_TOKEN_HERE
   ```

3. **Запустите Django:**
   ```bash
   python manage.py runserver
   ```

4. **В другом терминале запустите ngrok:**
   ```bash
   ngrok http 8000
   ```

5. **Используйте URL!**
   - Вы увидите что-то вроде: `https://abc123.ngrok.io`
   - Этот URL работает из интернета!
   - Поделитесь им - сайт доступен везде!

### 📱 Вариант 2: Доступ из локальной сети

Для доступа с других устройств в вашей сети:

**Windows:**
```bash
quick_start.bat
```

**Linux/Mac:**
```bash
chmod +x quick_start.sh
./quick_start.sh
```

**Или вручную:**
```bash
python manage.py runserver 0.0.0.0:8000
```

Затем откройте на другом устройстве: `http://ВАШ-IP:8000`

### 🎯 Вариант 3: Автоматический скрипт

```bash
python start_public_server.py
```

Скрипт автоматически:
- Покажет ваш IP адрес
- Запустит сервер для сетевого доступа
- Покажет все доступные URL

## 📋 Подготовка перед запуском

1. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Примените миграции:**
   ```bash
   python manage.py migrate
   ```

3. **Соберите статику:**
   ```bash
   python manage.py collectstatic --noinput
   ```

4. **(Опционально) Сгенерируйте тестовые данные:**
   ```bash
   python generate_kazakhstan_data.py
   ```

## 🔒 Безопасность

### Для публичного доступа:

**Создайте файл `.env` в корне проекта:**
```
SECRET_KEY=ваш-секретный-ключ-здесь
DEBUG=True
ALLOWED_HOSTS=*
DB_NAME=uniquestus
DB_USER=postgres
DB_PASSWORD=ваш_пароль
DB_HOST=localhost
DB_PORT=5432
```

(Файл `.env` уже добавлен в .gitignore и не попадет в Git)

## ✅ Готово!

Теперь ваш сайт доступен:
- ✅ Локально: http://127.0.0.1:8000
- ✅ Из вашей сети: http://ВАШ-IP:8000
- ✅ Из интернета (с ngrok): https://xxxx.ngrok.io

## 📚 Дополнительная информация

- **Подробная инструкция:** README_PUBLIC.md
- **Полное развертывание:** DEPLOY_PUBLIC.md
- **Улучшения проекта:** README_IMPROVEMENTS.md

## 🆘 Помощь

Если что-то не работает:
1. Проверьте, что PostgreSQL запущен
2. Убедитесь, что порт 8000 свободен
3. Проверьте файрвол Windows/Linux
4. Посмотрите логи Django

---

**Удачи! 🎉**

