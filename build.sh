#!/bin/bash
# Скрипт сборки для облачных платформ

echo "🔨 Начало сборки проекта..."

# Установка зависимостей уже выполнена платформой
# Собираем статику
python manage.py collectstatic --noinput

# Применяем миграции
python manage.py migrate --noinput

echo "✅ Сборка завершена!"

